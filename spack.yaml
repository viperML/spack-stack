spack:
  view: true
  concretizer:
    unify: true

  specs:
  - cmake
  - py-torch

  packages:
    all:
      require:
        - target=x86_64
        - +cuda
        - cuda_arch=60

  mirrors:
    gh-buildcache:
      url: oci://ghcr.io/viperml/spack-stack
      signed: false
