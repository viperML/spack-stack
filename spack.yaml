spack:
  view: true
  concretizer:
    unify: true

  specs:
  - cmake

  packages:
    all:
      require:
        - target=x86_64

  mirrors:
    gh-buildcache:
      url: oci://ghcr.io/viperml/spack-stack
      signed: false
